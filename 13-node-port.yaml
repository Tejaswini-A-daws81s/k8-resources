apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selectors:
    project: expense
    module: backend
    environment: dev
  ports:
    - name: node-port
      protocol: TCP
      port: 80
      targetPort: 80