kind: Service
apiVersion: v1

metadata:
  name: nginx-node
spec:
  selectors:
    project: expense
    module: backend
    environment: dev
  ports:
    - name: nginx-node-port
      protocol: TCP
      port: 80
      targetPort: 80